xzapple里的图片是从images里的图片中随机抽取，以此来训练模型

使用了xzapple文件里的图片，基于yolov8n.pt来训练，得到了train0里的结果

使用训练得到的best.pt作为检测模型

在test.py中，初步完成了一个检测系统。

功能操作
一. 图片检测
1. 点击"图片检测"按钮
2. 选择要检测的图片文件
3. 系统将显示检测结果，包括：
   • 每个苹果的边界框和成熟度分类
   • 各类别苹果的数量统计
   • 处理帧率（FPS）信息

二. 实时摄像头检测
1. 点击"摄像头实时检测"按钮
2. 系统将开启默认摄像头进行实时检测
3. 按键盘上的Q键退出摄像头检测模式

成熟度判定标准
系统采用融合判定策略，结合YOLO模型识别结果和HSV色彩空间分析：

成熟度等级 颜色标识 判定标准

未成熟（immature） 蓝色 YOLO识别为未成熟，或饱和度均值<80

成熟（mature） 绿色 YOLO识别为成熟且饱和度均值≥80

过熟（overmature） 红色 YOLO识别为过熟且饱和度均值≥80

算法原理
YOLO目标检测

系统基于YOLOv8架构，该架构在保持高检测速度的同时显著提升了检测精度。YOLOv8通过引入新的骨干网络和检测头设计，优化了特征提取和目标定位能力。

成熟度融合判定算法
def maturity_fusion(cls_name, s_mean):
    # 结合目标检测结果和饱和度特征进行综合判定
    final_class = cls_name
    if cls_name != "immature":
        if s_mean < 80:  # 基于饱和度阈值的二次验证
            final_class = "immature"
    return final_class
